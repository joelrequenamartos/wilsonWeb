---
import { getCollection } from 'astro:content';
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import EbookSection from '../components/EbookSection.astro';

const tours = (await getCollection('tours')).sort((a, b) => a.data.order - b.data.order);
---
<Layout title="Ebook — Silver Tours NY" description="La guía secreta de Nueva York escrita por Wilson: rutas fuera del circuito turístico habitual, mapa descargable y recomendaciones barrio por barrio.">
  <Header />
  <main id="main">
    <div class="wrap" style="padding-top:28px;">
      <a class="tour-back" href="/"><svg class="icon" style="transform:rotate(180deg)"><use href="#i-arrow" /></svg>Volver al inicio</a>
    </div>
    <EbookSection />
  </main>
  <Footer tours={tours} />
</Layout>
